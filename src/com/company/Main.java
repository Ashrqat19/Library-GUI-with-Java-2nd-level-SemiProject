package com.company;

import com.company.GUI.loginGui;

import javax.swing.*;

public class Main   extends JFrame implements arraylist{

    //frame
    static JFrame f;

    //lists
    static JList b;



        public static void main(String[] args) {
          //  new mainCode();


           // login open =new login();


            loginGui first =new loginGui();
           // first.run();
           /*

        Scanner sc = new Scanner(System.in);
            //create a new frame
            f = new JFrame("frame");

            //create a object
            Main s=new Main();

            //create a panel
            JPanel p =new JPanel();

            //create a new label
            JLabel l= new JLabel("select the day of the week");

            //String array to store weekdays

            MainBook[] userShow =new MainBook[allMainBook.size()];
            for (int i = 0; i <allMainBook.size() ; i++) {
                userShow[i]=allMainBook.get(i);
            }

            //create list
            b= new JList(userShow);

            //set a selected index
            b.setSelectedIndex(2);

            //add list to panel
            p.add(b);

            f.add(p);

            //set the size of frame
            f.setSize(400,400);

            f.show();
     */
        }


    }
